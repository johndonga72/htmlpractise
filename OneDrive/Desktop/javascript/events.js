const myfunction = function () {
    document.querySelector('body').style.backgroundColor = "green"
    document.querySelector('body').textContent = 'Light Theme'
};

const change = function (item) {
    item.style.backgrondColor = 'yellow';
}